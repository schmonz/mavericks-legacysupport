# Release notes

One file per release, named `<full-version>.md`, e.g. `1.5.2-mavericks.1.md`.
Its contents become the Sparkle appcast `<description>` and the GitHub Release body.
A release with no matching file gets auto-generated GitHub Release notes and a generated default in the Sparkle appcast.
